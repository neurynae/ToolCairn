// ToolPilot Indexer — GitHub crawler + health calculator + embedding processor
// Crawlers: github, npm, pypi, crates.io
// Processors: health-calculator, relationship-extractor, embedding-processor
// Implemented in Phase 2
export {};
